import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Preview = () => {
  return (
    <SafeAreaView className="flex-1 bg-white dark:bg-dark-background"></SafeAreaView>
  );
};

export default Preview;
